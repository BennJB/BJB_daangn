<nav class="navbar nav-global fixed-top navbar-expand-sm">
  <div class="container">
    <%= link_to products_path, class: 'navbar-brand' do %>
      <i class="material-icons ic-filter">arrow_back_ios</i>뒤로
    <% end %>
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <a href="#" class="btn btn-sm btn-warning disabled">채팅으로 거래하기</a>
      </li>
    </ul>
  </div>
</nav>

<div class="container container-sm container-detail">
  <%= image_tag @product.image.url, width: '100%' %>
  <h3 class="product-title"><%= @product.title %></h3>
  <h4 class="product-price" style="font-weight:bold;">
    <%= number_to_currency(@product.price) %>
  </h4>
  <ul class="list-product-information">
    <!--
    <li class="list-item category">카테고리 <span>디지털</span></li>
    -->
    <li class="list-item catetgry">등록자 <span><%= @product.user.name %></span></li>
    <li class="list-item date">상품 등록 시간
      <span><time><%= time_ago_in_words(@product.created_at) %> 전</time></span>
    </li>
    <!--
    <li class="list-item car-model-year">차량 연식<span>3년</span></li>
    <li class="list-item car-mileage">주행 거리<span>1,299km</span></li>
    <li class="list-item car-smoking">판매자 흡연 여부<span>흡연자</span></li>
    -->
  </ul>
  <div class="description">
    <%= @product.description %>
  </div>
</div>
