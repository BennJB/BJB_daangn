<% @title = "당근마켓 - 중고 제품 상세!" %>

<nav class="navbar nav-global fixed-top navbar-expand-sm">
    <div class="container">
        <a class="navbar-brand" href="javascript:history.back()">
            <i class="material-icons ic-filter">arrow_back_ios</i>뒤로
        </a>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a href="" class="btn btn-sm btn-warning disabled">채팅으로 거래하기</a>
            </li>
        </ul>
</div>
</nav>

<div class="container container-sm container-detail">
    <%= image_tag @item.image.url, width: '100%' %>
    <h3 class="product-title"><%= @item.title %></h3>
    <p class="counting-view">조회 <%= @item.impressionist_count %></p>
    <h4 class="product-price" style="font-weight:bold;">
    <%= number_to_currency(@item.price) %>
    </h4>
    <ul class="list-product-information">
        <li class="list-item category">카테고리 <span><%= @item.category.title %></span></li>
        <li class="list-item date">상품 등록 시간 <span><time><%= time_ago_in_words(@item.created_at) %> 전</time></span></li>
        <% if @item.category_id == 1 %>
            <li class="list-item car-model-year">차량 연식 <span><%= @item.caryear %>년</span></li>
            <li class="list-item car-mileage">주행 거리 <span><%= @item.mileage %>km</span></li>
            <li class="list-item car-smoking">판매자 흡연 여부 <span><%= @smoking %></span></li>
        <% end %>    
    </ul>
    <div class="description">
        <p><%= @item.description %></p>
    </div>       
</div>